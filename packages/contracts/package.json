{
  "name": "@orkora/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Zod schemas and shared types - the source of truth for API shapes",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}

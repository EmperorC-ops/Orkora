{
  "name": "@orkora/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Typed API client used by web and mobile",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@orkora/contracts": "workspace:*",
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
